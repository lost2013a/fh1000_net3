20160923  ETH_InitStructure.ETH_SecondFrameOperate = ETH_SecondFrameOperate_Enable;	//开启的DMA处理第二帧功能，应该关闭
20160928  在PPS中断中补偿（-880ns）的延迟。另外中断中getTime();
20160929  1.handleFollowUp中case PTP_SLAVE:分支结束少了一个break，只是语法问题，无关逻辑关系;
		  2.加入做测试仪时的向PC发送数据的对应代码，以便理解协议栈，使用了Gx_PTP_Device条件编译。
		  3.做测试仪的时候updateDelay中也不能要求ofm_filt.n大于0，将会导致E2E断线重连网络延迟数据无法更新，相应代码重新编排。
20161014  在发送大于1524字节的网络数据包的时候，只会把数据填充一个DMA描述符的缓冲区内，导致数据错误。同时，在填写DMA描述符时，会分配多个DMA描述符，由此导致DMA描述链表出现
		  无法恢复的错误。修改DMA描述符填写部分的代码，只允许1个分配1个DMA描述符，测试3天无异常。
20161018  修改DMA描述符缓存区填充的代码，超长的网络数据包会分配到多个DMA描述符对应缓冲区		  
20161021  1.重新整理文件结构，删除官方开发板的多余代码
		  2.修改同步信号锁定条件，时间稳定后，改为每4次调整1次硬件时间。
20161031  1.修改ETH发送TXBUF数量为1个，发送时等时间戳超时状态，也要指向下一个地址
		  2.修改修改offset_time的滤波算法
		  3.注释掉//__IO ETH_DMADESCTypeDef *PTPdescriptor;
		  4.重新移植启动文件startup，去掉未用的FREEROTS支持
20161107  1.修改作主时FollowUp发送的时间戳为协议标准(sys发出时)，不再是收到自己SYS报文的时间		
20161108  1.修改写FLASH函数，移植正点原子代码
		  2.编写CAN配置功能，修改了原PTP全局变量结构。
20161127  1.修改CAN配置发送函数，减少CAN帧间隔，减少被高优先级CAN帧打断的几率。
		  2.修改RMII的频率源，关闭PLL2S
		  3.修改4个指示灯的定义，LED1=运行，LED2=同步，LED=PPS（收），LED4=锁定输出。
20161225  1.历元可通过CAN配置，值=0时用UTC时间，关掉UTCOFFSET选项，大于20时使用TAI时间，使用TAI时间做历元。
		  2.不再隔4秒才调整一次硬件时间。
20170106  1.初始化PTP协议，不再清指数平滑算法和PI算法的数据集。
		  2.初始化PTP后，首先带入一个标准的频率值。
20170110  1.去掉未使用的上1秒的全局变量。
20170112  1.重写FHGX的串口同步函数。
		  2.重写PTP测试端的串口配置函数。
		  3.重写PTP配置函数，改变PTP协议层或链路模式，只重启PTP协议栈。改变网络配置，会写FLASH和重启板卡。
20170214  1.增加FH1000的网络界面修改配置功能。